# ~/.bashrc mínimo para Render CLI 

# Exportar la API Key de Render 
export RENDER_API_KEY=tu_api_key_aqui 

# Asegurar que el PATH incluya binarios locales (opcional pero común) 
export PATH="$HOME/.local/bin:$PATH"